{
  "name": "realtypay",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm install --prefix frontend --legacy-peer-deps && npm start --prefix frontend",
    "start": "npm install --prefix frontend --legacy-peer-deps && npm start --prefix frontend",
    "build": "npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend"
  }
}
